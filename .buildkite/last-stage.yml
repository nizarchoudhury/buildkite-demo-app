steps:
    command:
      #- buildkite-agent artifact download "hello/hello" .
      #- chmod +x hello/hello
      #- ./hello/hello $(buildkite-agent meta-data get "name")
      - chmod +x script.sh
      - ./script.sh
